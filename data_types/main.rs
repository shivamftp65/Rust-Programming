fn main(){
    // let guess:u32 = "42".parse().expect("Not a number!");
    // println!("{guess}");

    // Scaler Types: Intgers, floating Point, characters, boolean 
    // Interger Types
    let a:i8  = -10;
    println!("The value a is {a}");

    // Floating_point Type
    let x:f32 = 10.98;
    let y:f64 = -15.883;

    println!("floating numbers are x: {x}, y: {y}");

    // Boolean Type
    let t = true;
    let f: bool = false; 
    println!("{t}: {f}");

    // character Types
    let ch = 'a';
    let c : char = '@';
    println!("{ch} : {c}");

}